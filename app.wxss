page {
  min-height: 100%;
  background: linear-gradient(180deg, #0b0b0c 0%, #121318 100%);
  color: #ffffff;
}

view,
text,
button,
image,
input,
scroll-view,
textarea {
  box-sizing: border-box;
}

.container {
  min-height: 100vh;
  padding: 28rpx 24rpx 40rpx;
}

.card,
.state-card {
  background: rgba(255, 255, 255, 0.08);
  border: 1px solid rgba(255, 255, 255, 0.14);
  border-radius: 28rpx;
  box-shadow: 0 16rpx 48rpx rgba(0, 0, 0, 0.18);
}

.state-card {
  margin: 28rpx 20rpx 0;
  padding: 32rpx 28rpx;
  font-size: 28rpx;
  color: rgba(255, 255, 255, 0.82);
  text-align: center;
}

.section-title {
  display: block;
  font-size: 40rpx;
  font-weight: 700;
}

.section-subtitle {
  display: block;
  margin-top: 10rpx;
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.7);
}

.btn-primary,
.btn-secondary {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  height: 84rpx;
  padding: 0 32rpx;
  border-radius: 999rpx;
  font-size: 28rpx;
}

.btn-primary {
  border: 0;
  background: linear-gradient(135deg, #667eea, #7c5cd6);
  color: #ffffff;
  font-weight: 600;
}

.btn-secondary {
  border: 1px solid rgba(255, 255, 255, 0.18);
  background: rgba(255, 255, 255, 0.08);
  color: #ffffff;
}

.btn-primary::after,
.btn-secondary::after {
  border: 0;
}
